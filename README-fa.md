# **⚡ IPWeaver**

[**🇺🇸 Click here for the English Version**](http://docs.google.com/README.md)

برنامه **IPWeaver** یک ابزار شبکه محلی و سریع است که برای خودکارسازی کشف آی‌پی‌های سالم و ترکیب آن‌ها با کانفیگ‌های پروکسی (V2Ray) طراحی شده است. این برنامه پینگ‌های همزمان ICMP را با پردازش گروهی لینک‌های کانفیگ ترکیب می‌کند تا بهترین کانفیگ‌ها را در چند ثانیه برای شما تولید کند.

## **✨ ویژگی‌ها**

* **اسکنر همزمان آی‌پی:** لیست بزرگی از آی‌پی‌ها را وارد کنید و پینگ واقعی (ICMP) آن‌ها را به صورت همزمان تست کنید.  
* **تولیدکننده گروهی کانفیگ:** کانفیگ‌های V2Ray خود (VMess, VLESS, Trojan, SS) را در یک لحظه روی چندین آی‌پی سالم اعمال و تکثیر کنید.  
* **تغییر پورت گروهی:** پورت تمام کانفیگ‌های خروجی را تنها با یک کلیک تغییر دهید.  
* **طراحی رابط کاربری دوگانه:** امکان جابه‌جایی بین حالت مدرن (پنجره‌ای) و حالت کلاسیک (تاریک و دو ستونه).  
* **نسخه دسکتاپ مستقل:** قابل استفاده به صورت یک فایل .exe مستقل بدون نیاز به مرورگر وب، و یا اجرای مستقیم با اسکریپت پایتون.

## **🚀 آموزش نصب و استفاده**

### **روش اول: استفاده از نسخه آماده (پیشنهادی)**

ما فایل آماده .exe را برای شما قرار داده‌ایم تا نیازی به نصب پایتون یا کامپایل کردن کدها نداشته باشید\!

۱. به بخش [**Releases**](http://docs.google.com/releases) در گیت‌هاب بروید.

۲. جدیدترین نسخه فایل IPWeaver.exe را دانلود کنید.

۳. فایل را اجرا کنید. برنامه در یک پنجره مستقل باز می‌شود و نیازی به نصب هیچ پیش‌نیازی نیست\!

### **روش دوم: اجرا از طریق سورس کد (برای برنامه‌نویسان)**

اگر ترجیح می‌دهید کد پایتون را مستقیماً اجرا کنید:

۱. سورس پروژه را دانلود کنید:

git clone https://github.com/rm-killer/IPWeaver.git  
cd IPWeaver

۲. پیش‌نیازهای پایتون را نصب کنید:

pip install fastapi uvicorn ping3 pywebview

۳. برنامه را اجرا کنید:

python main.py

*رابط کاربری به صورت خودکار در مرورگر پیش‌فرض شما باز خواهد شد.*

## **💖 حمایت مالی از پروژه**

اگر این ابزار برای شما مفید بوده و باعث صرفه‌جویی در زمانتان شده است، می‌توانید از توسعه آن حمایت کنید\!

**آدرس کیف پول اتریوم (ETH):**

0x961CAdFe78395AEE95CC31a4BC3890E3e4dF7E59

*توسعه داده شده توسط [@rm-killer](https://github.com/rm-killer)*